[Unit]
Description=Kubernetes proxy
Requires=network-online.target
After=network-online.target

[Service]
Restart=always
RestartSec=10s
ExecStart=/opt/bin/hyperkube proxy \
    --master=http://{{kubernetes_master}}:{{kubernetes_apiserver_insecure_port}} \
    --v=2 \
    --logtostderr=true
